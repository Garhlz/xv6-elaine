## 指导步骤：一个合理的攻击计划

实验手册给出的计划非常好，我们来把它细化成具体的、可操作的步骤。

第 1 步：改造 fork -> uvmcopy，实现“共享”

目标: 修改 uvmcopy() (kernel/vm.c)，让它不再为子进程分配新的物理内存并复制内容，而是直接共享父进程的物理页。

在哪里修改: kernel/vm.c 中的 uvmcopy() 函数。

指导:

    uvmcopy 的原始逻辑是：遍历父进程的页表，对于每一个映射的页面，kalloc 一个新页面，然后 memmove 复制内容。

    你的新逻辑应该是：

        同样遍历父进程的页表 (for 循环和 walk 调用保持不变)。

        当找到一个父进程的有效 PTE 时，不要调用 kalloc。

        直接从父进程的 PTE 中提取出物理地址 (pa) 和权限标志 (flags)。

        清除 flags 中的 PTE_W 位（flags &= ~PTE_W），强制页面变为只读。

        调用 mappages()，将同一个物理地址 pa，用新的只读权限 flags，映射到子进程的页表中。

        关键: 父进程的页表也需要修改！你必须通过 walk 找到父进程中同一个虚拟地址的 PTE，并同样清除它的 PTE_W 位。否则，只有子进程是只读的，父进程依然可写，这就不是共享了。

第 2 步：管理引用计数，避免“过早释放”

目标: 一个物理页面现在可能被多个进程共享，我们必须追踪每个页面的“共享者”数量，只有当最后一个共享者不再需要它时，才能真正释放这块物理内存。

在哪里修改: kernel/kalloc.c 是最核心的地方，uvmcopy 和 uvmunmap 等也需要配合修改。

指导:

    数据结构: 在 kalloc.c 中，你需要一个地方来存储每个物理页的引用计数。一个简单的全局数组是可行的。

        int ref_counts[PHYSTOP / PGSIZE]; 数组的大小需要覆盖所有可用于分配的物理内存。

        PHYSTOP / PGSIZE 会得到物理页的总数，可以用物理页号 (PPN) 作为数组索引。

    修改 kalloc.c:

        kalloc(): 当分配一个新页面时，应该将其引用计数初始化为 1。

        kfree(): 这是关键修改！ kfree 不能再立即释放页面。它应该先将页面的引用计数减 1。只有当引用计数减到 0 时，才真正地把页面链接回 freelist。

    修改 vm.c:

        uvmcopy(): 在你上一步共享页面时，你让子进程也指向了父进程的物理页。这时，这个物理页的引用者多了一个。所以，你需要在这里将该物理页的引用计数加 1。

        uvmunmap(): 当一个进程解除对某个页面的映射时（比如进程退出或 sbrk 缩小内存），它就不再是这个页面的引用者了。所以，你需要在 uvmunmap 中，在释放页面之前，先将该物理页的引用计数减 1（这部分逻辑可以封装在 kfree 内部完成）。

第 3 步：处理缺页异常，实现“写时复制”

目标: 在 usertrap 中捕捉到对 COW 页面的写操作，并执行真正的复制。

在哪里修改: kernel/trap.c 中的 usertrap() 函数。

指导:

    在 usertrap 的 if/else if/else 分诊逻辑中，你需要增加对缺页异常的判断。

        r_scause() 的值为 13 (读缺页) 或 15 (写缺页)。我们这里只关心写缺页 (Store page fault)。

    当检测到是写缺页异常时：

        获取故障地址: r_stval() 寄存器里存放着导致异常的那个虚拟地址。你需要对它进行页对齐。

        检查是否为 COW 页: 找到这个故障地址对应的 PTE（使用 walk）。你需要一种方法来判断这个 PTE 是不是一个 COW 页。一个好方法是利用 RISC-V PTE 中为软件保留的 RSW 位。在第 1 步 uvmcopy 中共享页面时，除了清除 PTE_W，你还可以设置一个自定义的 PTE_COW 标志位。在这里，你就检查这个标志位。

        如果不是 COW 页: 说明是真正的非法写入，按原逻辑杀死进程。

        如果是 COW 页:
        a.  检查引用计数: 获取这个页面的物理地址，并查看它的引用计数。如果计数为 1，说明只有当前进程在用它，你不需要复制！直接在 PTE 中把 PTE_W 位加上，然后返回即可（这是一个重要的优化）。
        b.  分配与复制: 如果引用计数大于 1，调用 kalloc() 分配一个新的物理页面。然后，将旧页面的内容完整地复制 (memmove) 到新页面中。
        c.  更新页表: 修改当前进程的 PTE，让它指向这个新的、私有的页面，并设置权限为可写 (PTE_W)，同时清除 PTE_COW 标志。
        d.  维护引用计数: 旧页面的引用计数需要减 1，新页面的引用计数初始化为 1（kalloc 已经做了）。

这个实验的挑战在于，你需要同时修改内存分配、fork 实现和陷阱处理这三个内核的核心部分，并保证它们之间的数据（引用计数）和逻辑（标志位）能够正确地协同工作。祝你顺利！